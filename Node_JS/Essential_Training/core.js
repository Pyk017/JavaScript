// const path = require('path');
// const util = require('util');
// const v8 = require('v8');

// console.log(v8.getHeapStatistics())


// util.log( path.basename(__filename)); // works similar to console.log() but shows timestamp of the output as well.
// util.log("^ The name of the current file");

// to get only the functions from the module. we use destructuring method
// const { basename } = require('path');
// const { log } = require('util');   // provides log function of util module to work upon.
// log(basename(__filename));